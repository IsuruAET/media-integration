{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run dev",
    "build": "lerna run build",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^6.6.1"
  }
}
